import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#ED213A",
        },
        text: {
            primary: "#FFFFFF",
            secondary: "#79797B",
        },
        background: {
            default: "#1A1A1D",
        }
    }
});